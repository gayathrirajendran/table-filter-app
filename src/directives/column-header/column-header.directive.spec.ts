import { ColumnHeaderDirective } from './column-header.directive';

describe('ColumnHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ColumnHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
