import { ColumnCellDirective } from './column-cell.directive';

describe('ColumnCellDirective', () => {
  it('should create an instance', () => {
    const directive = new ColumnCellDirective();
    expect(directive).toBeTruthy();
  });
});
